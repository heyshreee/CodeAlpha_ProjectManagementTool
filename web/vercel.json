{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://<api-vercel-domain>/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}